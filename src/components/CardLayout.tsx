import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  cardLayout: {
    borderColor: 'white',
    borderWidth: 10,
    height: '85%',
    width: '100%',
  },
});

export default styles.cardLayout;
