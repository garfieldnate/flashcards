import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  cardLayout: {
    width: "100%",
    height: "85%",
    borderColor: "white",
    borderWidth: 10,
  }
});

export default styles.cardLayout;
